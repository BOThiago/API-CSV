import { MigrationErrorMessage, MigrationMessage } from '../types';
import { Output } from './index';
export declare class MigrationPrinter {
    out: Output;
    constructor(out: Output);
    printMessages(migrationMessages: MigrationMessage[]): void;
    printErrors(errors: MigrationErrorMessage[]): void;
    private printMigrationMessage(migrationMessage, index);
    private getType(message);
    private getSymbol(type);
}

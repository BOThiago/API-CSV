import { Config } from './Config';
import { Output } from './Output/index';
import 'isomorphic-fetch';
import { AuthTrigger } from './types';
import { Client } from './Client/Client';
export declare class Auth {
    out: Output;
    config: Config;
    authTrigger: AuthTrigger;
    client: Client;
    constructor(out: Output, config: Config, client: Client);
    setAuthTrigger(authTrigger: AuthTrigger): void;
    ensureAuth(): Promise<boolean>;
    setToken(token: string): Promise<void>;
    requestAuthToken(): Promise<string>;
    validateAuthToken(token: string): Promise<string | null>;
}

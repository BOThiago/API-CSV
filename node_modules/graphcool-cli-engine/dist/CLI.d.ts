import { Command } from './Command';
import { Config } from './Config';
import { RunOptions } from './types';
export declare class CLI {
    config: Config;
    cmd: Command;
    constructor({config}?: {
        config?: RunOptions;
    });
    run(): Promise<void>;
    flush(): Promise<{} | void>;
    readonly cmdAskingForHelp: boolean;
    readonly Help: any;
    private getCommandId(argv);
}
export declare function run({config}?: {
    config?: RunOptions;
}): Promise<void>;

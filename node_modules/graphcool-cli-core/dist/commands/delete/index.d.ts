import { Command, Flags } from 'graphcool-cli-engine';
export default class Delete extends Command {
    static topic: string;
    static description: string;
    static hidden: boolean;
    static group: string;
    static flags: Flags;
    run(): Promise<void>;
    private askForConfirmation(projects);
}

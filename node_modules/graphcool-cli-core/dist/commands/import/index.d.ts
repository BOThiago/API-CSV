import { Command, Flags } from 'graphcool-cli-engine';
export default class Import extends Command {
    static topic: string;
    static description: string;
    static flags: Flags;
    run(): Promise<void>;
    import(source: string, id: string): Promise<void>;
}

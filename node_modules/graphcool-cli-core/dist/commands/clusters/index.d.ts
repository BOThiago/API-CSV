import { Command, Flags } from 'graphcool-cli-engine';
export default class Clusters extends Command {
    static topic: string;
    static description: string;
    static flags: Flags;
    run(): Promise<void>;
    private nameSelector();
    private endpointSelector();
    private masterTokenSelector();
}

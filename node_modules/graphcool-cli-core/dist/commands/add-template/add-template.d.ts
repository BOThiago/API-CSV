import { Command } from 'graphcool-cli-engine';
import 'isomorphic-fetch';
export default class AddTemplate extends Command {
    static topic: string;
    static description: string;
    static args: {
        name: string;
        required: boolean;
    }[];
    static help: string;
    run(): Promise<void>;
    extractInfo(moduleUrl: string): Promise<{
        repoName: string;
        subPath: string;
        moduleDirName: string;
    }>;
    mergePackageJsons(source: string): Promise<void>;
    private checkUrl(repoName, subPath, moduleUrl);
    private npmInstall();
}

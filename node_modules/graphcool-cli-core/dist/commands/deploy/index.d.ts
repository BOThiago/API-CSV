import { Command, Flags } from 'graphcool-cli-engine';
export default class Deploy extends Command {
    static topic: string;
    static description: string;
    static group: string;
    static allowAnyFlags: boolean;
    static help: string;
    static flags: Flags;
    private deploying;
    run(): Promise<void>;
    private createProject(isLocal, cluster, name, alias?, region?);
    private prettyTime(time);
    private deploy(projectIsNew, targetName, projectId, isLocal, force, projectName, cluster);
    private printEndpoints(projectId);
    private clusterSelection();
    private serviceNameSelector(defaultName);
    private targetNameSelector(defaultName);
    private dryRun();
}
export declare function isValidProjectName(projectName: string): boolean;

import { Command, Flags, ProjectDefinition } from 'graphcool-cli-engine';
export default class Init extends Command {
    static topic: string;
    static description: string;
    static group: string;
    static help: string;
    static flags: Flags;
    static args: {
        name: string;
        description: string;
    }[];
    run(): Promise<void>;
    interactiveInit(): Promise<ProjectDefinition>;
    private projectSelection();
    private exampleSelection();
    private askForConfirmation(folder);
}

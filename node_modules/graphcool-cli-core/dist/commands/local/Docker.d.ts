/// <reference types="lodash" />
import { Output, Config, Environment } from 'graphcool-cli-engine';
export default class Docker {
    out: Output;
    env: Environment;
    config: Config;
    clusterName: string;
    ymlPath: string;
    envPath: string;
    envVars: {
        [varLemieuxName: string]: string;
    };
    constructor(out: Output, config: Config, env: Environment, clusterName: string);
    readonly hostName: string;
    init(): Promise<void>;
    up(): Promise<Docker>;
    ps(): Promise<Docker>;
    start(): Promise<Docker>;
    stop(): Promise<Docker>;
    restart(): Promise<Docker>;
    pull(): Promise<Docker>;
    private run(...argv);
    private format(data);
    getDockerEnvVars(): _.Dictionary<string>;
    parseEnv(src: string): _.Dictionary<string>;
}

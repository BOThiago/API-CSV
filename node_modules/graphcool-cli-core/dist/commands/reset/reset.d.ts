import { Command, Flags } from 'graphcool-cli-engine';
export default class Reset extends Command {
    static topic: string;
    static description: string;
    static flags: Flags;
    run(): Promise<void>;
    private askForConfirmation(serviceName);
}

import { Command, Flags } from 'graphcool-cli-engine';
export default class FunctionLogs extends Command {
    static topic: string;
    static description: string;
    static group: string;
    static flags: Flags;
    run(): Promise<void>;
    private provideAllFunctionLogs(id, tail?);
    private provideSingleFunctionLogs(id, functionName, tail?);
    private prettifyLogs(logs);
    private lambdaToArray(logs);
}

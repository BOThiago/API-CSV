import { Command, Flags, ProjectInfo, Project } from 'graphcool-cli-engine';
export default class InfoCommand extends Command {
    static topic: string;
    static description: string;
    static group: string;
    static flags: Flags;
    run(): Promise<void>;
    infoMessage: (info: ProjectInfo, envName: string, projects: Project[], localPort?: number | undefined) => string;
}
